# Secrets — never commit
.env
*.pem
*.key

# Python
.venv/
__pycache__/
*.pyc

# Pipeline output (rendered cards)
output/

# OS
.DS_Store

# Local-only archive moved from ~ during the 2026-07-14 root cleanup — private notes/backups, never commit
Athena_moved_from_root/
